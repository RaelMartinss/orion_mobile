name: orion_mobile
description: "Orion Mobile — assistente de voz para Android"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.0
  speech_to_text: ^7.0.0
  flutter_tts: ^4.2.0
  shared_preferences: ^2.3.0
  permission_handler: ^11.3.0
  url_launcher: ^6.3.0
  android_intent_plus: ^5.0.0
  camera: ^0.11.0
  livekit_client: ^2.7.0
  package_info_plus: ^8.0.0
  path_provider: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
